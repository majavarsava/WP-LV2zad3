<!DOCTYPE html>
<html lang="hr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Galerija slika - LV1 iz web programiranja">
    <meta name="author" content="Maja Varšava">
    <meta name="keywords" content="web programiranje, galerija, slike, HTML, projekt">
    
    <link rel="stylesheet" href="/styles/style_slike.css">
    <link rel="stylesheet" href="/styles/style.css">

    <title>Galerija slika - LV1 Web Programiranje</title>
</head>
<body>
    <header>
        <h1>Dobrodošli u galeriju slika</h1>
    </header>

    <nav aria-labelledby="primarna-navigacija">
        <ul class="nav-menu">
            <li class="dropdown">
                <a href="#" class="dropbtn">Menu</a>
                <div class="dropdown-content">
                    <a href="/">Home</a>
						<a href="/slike">Galerija</a>
						<a href="/grafikon">Grafikon</a>
                </div>
            </li>
        </ul>
    </nav>

    <section class="galerija"> 
    <section class="img-gallery-magnific">
        <h1>Galerija slika</h1>
        <p></p>

        <% images.forEach(image =>{ %>
        <figure class="magnific-img">
            <a class="image-popup-vertical-fit" href="#<%= image.id%>">
                <img src="<%= image.url%>" alt="<%= image.title %>" loading="lazy">
            </a>
            <figcaption><%= image.title %></figcaption>
        </figure>
        <% });%>

        <% images.forEach(image => { %>
            <section id="<%= image.id %>" class="lightbox">
                <img src="<%= image.url %>" alt="<%= image.title %>">
                <a href="# " class="close">&times;</a>
            </section>
        <% }); %>
    </section>
    </section>

	<footer>
        <p>&copy; 2025. Web Programiranje. Sva prava pridržana.</p>
    </footer>
</body>
</html>